# RQData Fetch — Python dependencies
# Python >= 3.9 required (zoneinfo is stdlib since 3.9)
#
# Install into an isolated venv (recommended):
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

rqdatac>=2.0
pandas>=1.5,<3.0
numpy>=1.21,<2.0
